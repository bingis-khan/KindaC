#$ [env expansion] I don't remember what this was for, but its name (error name) was "constructor unions not unifying".

id (x)
	return x

uni (l asd, r asd)
	return

Callback
	Callback (Int -> Int)

ff = Callback(id)

f (x aaa)
	printn (n bbb) -> bbb
		print n
		print x
		return n

	uni(Callback(printn), ff)
	return

f(1)
f(True)
