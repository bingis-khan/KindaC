#$ [env expansion] Env expansion works.
# 123


uni (l a, r a) -> Unit
	return

f = :123

ff (x) -> Unit
	gg (y) -> Unit
		sumtin ()
			print x
			print y
			return 123

		uni(f, sumtin)
		return

	gg(True)
	gg(Unit)

	return

ff(123)
ff(True)
ff(Unit)

print f()
