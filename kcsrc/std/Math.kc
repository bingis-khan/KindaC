use Iter
	to
	reduce

min (l Int, r Int) -> Int
	if l < r
		return l
	else
		return r

max (l Int, r Int) -> Int
	if l > r
		return l
	else
		return r

pow (base, p):
	0 to (p - 1) reduce(1, (item, x): x * base)
