class Dupsko
	f (n _) -> Int


m = 5
inst Dupsko Int
	f (n Int) -> Int
		return n + m

inst Dupsko Unit
	f (n Unit) -> Int
		return 6969 + f(123) + f(8008135)

inst Dupsko Bool
	f (n Bool) -> Int
		return 1 + f(Unit)

print f(True)
