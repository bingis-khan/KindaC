#$ This is related to the issue of mutating outside of closure. The only problem here is that I don't yet support mutation outside of closures. Whether I add it is also up for debate.

id (x)
	return x

uni (l asd, r asd)
	return

nuttin ()
	print 123
	return

ff = nuttin

f (x a) -> Unit
	g (y b) -> Unit
		printn ()
			print x
			print y
			return

		ff <= printn
		return

	g(Unit)
	g(420)
	return

f(Unit)
f(420)
f(True)
print 69420
